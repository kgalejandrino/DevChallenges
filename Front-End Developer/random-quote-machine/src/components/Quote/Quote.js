import React from 'react';

import './Quote.css';

const quote = () => {
    return (
        <div className="Quote">
            <p>"The first rule of any technology used in a business is that automation applied to an efficient operation will magnify the efficiency. The second is that automation applied to an inefficient operation will magnify the ineffeciency."</p>
        </div>
    )
}

export default quote;